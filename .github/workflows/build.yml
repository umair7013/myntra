# name: Build

# on:
#   push:
#     branches:
#       - main


# jobs:
#   build:
#     name: Build
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v2
#         with:
#           fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis
#       - uses: sonarsource/sonarqube-scan-action@master
#         env:
#           SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
#           SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
#       - name: NPM Install
#         run: npm install 

#       - name: Docker Login
#         run: docker login -u ${{ secrets.DOCKERHUB_USERNAME }} -p ${{ secrets.DOCKERHUB_TOKEN }}

#       - name: Docker Scout Scan
#         run: |
#          docker-scout quickview fs://.
#          docker-scout cves fs://.

#       - name: Docker build and push
#         run: |
#          docker build -t myntra .
#          docker tag myntra aliumair0/myntra:latest
#          docker login -u ${{ secrets.DOCKERHUB_USERNAME }} -p ${{ secrets.DOCKERHUB_TOKEN }}
#          docker push aliumair0/myntra:latest
#        env:
#          DOCKER_CLI_ACI: 1
#       - name: Docker Scout Image Scan
#         run: |
#          docker-scout quickview aliumair0/myntra:latest
#          docker-scout cves aliumair0/myntra:latest

name: Build

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: NPM Install
        run: npm install

      - uses: sonarsource/sonarqube-scan-action@master
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}

      - name: Docker Login
        run: echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

      - name: Docker build and push
        env:
          DOCKER_CLI_ACI: 1
        run: |
          docker build -t myntra:latest .
          docker tag myntra:latest aliumair0/myntra:latest
          docker push aliumair0/myntra:latest

            - name: Docker Login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Docker Build and Push
        run: |
          docker build -t myntra:latest .
          docker tag myntra:latest aliumair0/myntra:latest
          docker push aliumair0/myntra:latest

      - name: Docker Scout Quick View
        uses: docker/scout-action@v1
        with:
          command: quickview
          image: aliumair0/myntra:latest

      - name: Docker Scout CVEs
        uses: docker/scout-action@v1
        with:
          command: cves
          image: aliumair0/myntra:latest
          only-fixed: true
      # - name: Install Docker Scout
      #   run: |
      #     curl -fsSL https://raw.githubusercontent.com/docker/scout-cli/main/install.sh | sh
      #     # Alternative agar upar wala na chale:
      #     # sudo curl -fsSL https://github.com/docker/scout-cli/releases/latest/download/docker-scout -o /usr/local/bin/docker-scout
      #     # sudo chmod +x /usr/local/bin/docker-scout


      # - name: Docker Scout Image Scan
      #   run: |
      #     docker-scout quickview aliumair0/myntra:latest
      #     docker-scout cves aliumair0/myntra:latest
      


      
